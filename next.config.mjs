/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  basePath: "/music-theory",
  output: "export",
  assetPrefix: "/music-theory/"
};

export default nextConfig;
